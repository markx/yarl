{
  "name": "yarl",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "shadow-cljs watch app",
    "build": "npm run clean && shadow-cljs release app && cp -R public/ dist",
    "clean": "rm -rf public/js/"
  },
  "devDependencies": {
    "shadow-cljs": "~2.8.66"
  },
  "dependencies": {
    "rot-js": "^2.1.1"
  }
}
